[project]
name = "voice-input"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "numpy",
    "pyaudio",
    "torch",
    "torchaudio",
    "alicepi-proto @ ../proto_defs",
]
