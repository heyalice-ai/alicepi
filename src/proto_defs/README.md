# AlicePi Proto Definitions

Shared protobuf messages for AlicePi services. Install via the path dependency declared in each service (`alicepi-proto @ ../proto_defs`) and import `alicepi_proto.vad_pb2` for the raw message classes or `alicepi_proto.vad` for framing helpers.
